package leetcode;

import java.util.List;

/**
 * 单次拆分
 * 一个单次的分解数
 * <p>
 * 暴力递归+动态规划（存在枚举行为，O(N^3)）+ 前缀树（利用前缀树剪枝）
 */
public class Problem75_Code139 {
    public boolean wordBreak(String s, List<String> wordDict) {
        return false;
    }
}
